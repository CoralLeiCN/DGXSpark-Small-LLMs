# 2026-09-03T21:54:10Z — Four-worker JIT completed and the service became healthy

Run ID: `RUN-0006`

- Status: resolved
- Phase: engine startup and health check
- Related turns:
  [JIT OOM](2026-09-03T21-18-43Z-flashinfer-jit-oom.md),
  [serialized JIT](2026-09-03T21-37-58Z-serialized-jit-follow-up.md), and
  [early health probe](2026-09-03T21-16-19Z-health-before-readiness.md)
- Repo revision: `c520931` (`dirty`)
- Host/GPU: DGX Spark, NVIDIA GB10, 121.69 GiB unified memory, driver
  `580.173.02`, ARM64
- Container: `dgxspark/nemotron-3-nano-30b-a3b-nvfp4-sglang:0.1.0`
- Engine: SGLang `0.5.15.post1`, FlashInfer `0.6.12`
- Model: `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4`, revision
  `6efb4a2a1c1fa277ce7b3df7a1416255011b1c99`, NVFP4

## Command

```bash
scripts/serve nvidia/nvidia-nemotron-3-nano-30b-a3b-nvfp4 \
  --engine sglang
curl --fail --silent --show-error http://127.0.0.1:30000/health
```

The Compose environment set `MAX_JOBS=4`.

## Error Or Observation

No new error occurred. Ninja compiled and linked all 96 fused-MoE objects,
FlashInfer autotuning completed, and SGLang captured prefill and decode CUDA
graphs.

## Diagnosis

The earlier OOM was caused by unbounded JIT concurrency, and the earlier health
reset was a readiness race before Uvicorn had started. Neither required a
different engine or model configuration.

## Fix Or Change

Use a four-worker JIT limit and named volumes for `/root/.cache/flashinfer` and
`/root/.cache/sglang`. Wait for Docker health rather than treating a published
port as readiness.

## Verification

```bash
curl --fail --silent --show-error http://127.0.0.1:30000/health
docker inspect dgxspark-nemotron-3-nano-sglang-1
```

SGLang reported readiness at `21:54:06Z`, `/health` returned HTTP 200 at
`21:54:10Z`, and Docker reached `healthy` with zero restarts and
`OOMKilled=false`.

## Lesson

Keep first-start health grace periods long enough for model loading, JIT
compilation, autotuning, and graph capture. Persist compiled artifacts and gate
validation on actual readiness.

## Next Step

Run the OpenAI-compatible inference validator.
